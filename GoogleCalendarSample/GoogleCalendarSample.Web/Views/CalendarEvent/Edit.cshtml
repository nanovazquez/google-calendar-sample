@model GoogleApiUtils.GoogleCalendarApi.CalendarEvent
@{
    ViewBag.Title = "Edit Event";
}
<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
    <h2>Edit an existing event.</h2>
</hgroup>

@using (Html.BeginForm("Edit", "CalendarEvent", FormMethod.Post) )
{
    <fieldset>
        <legend>Edit an event of your calendar</legend>

        @Html.ValidationSummary()
        @Html.HiddenFor(m => m.Id)
        @Html.HiddenFor(m => m.Attendees)
        <ol>
            <li>
                @Html.LabelFor(m => m.CalendarId)
                @Html.TextBoxFor(m => m.CalendarId)
            </li>
            <li>
                @Html.LabelFor(m => m.Title)
                @Html.TextBoxFor(m => m.Title)
            </li>
            <li>
                @Html.LabelFor(m => m.Location)
                @Html.TextBoxFor(m => m.Location)
            </li>
            <li>
                @Html.LabelFor(m => m.Description)
                @Html.TextAreaFor(m => m.Description, new { Style = "width:300px;" } )
            </li>
            <li>
                @Html.LabelFor(m => m.StartDate)
                @Html.EditorFor(m => m.StartDate)
            </li>
            <li>
                @Html.LabelFor(m => m.EndDate)
                @Html.EditorFor(m => m.EndDate)
            </li>
            <li>
                @Html.LabelFor(m => m.ColorId)
                @Html.DropDownList("ColorId", (SelectList)ViewBag.Colors, new { Style = "width:310px; height: 25px; font-size: 1.2em" })
            </li>
        </ol>
        <input type="submit" value="Edit" />
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}